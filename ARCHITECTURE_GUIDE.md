# TraceRoot Replit Architecture & Setup Flow

## 🏗️ System Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                        INTERNET / PUBLIC                           │
│                                                                     │
│  Reviewers → https://your-project.replit.dev ← Your Replit URL   │
└────────────────────────────────────┬─────────────────────────────┘
                                      │
                                      │ HTTPS
                                      ▼
┌────────────────────────────────────────────────────────────────────┐
│                     REPLIT.COM (FREE HOSTING)                      │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │ Virtual Machine / Container                                 │ │
│  │                                                              │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │ PORT 3000: Next.js Application                        │ │ │
│  │  │ ┌──────────────────────────────────────────────────┐  │ │ │
│  │  │ │ Frontend UI (React Components)                  │  │ │ │
│  │  │ │ • Login                                         │  │ │ │
│  │  │ │ • Dashboard                                     │  │ │ │
│  │  │ │ • Blockchain Operations                         │  │ │ │
│  │  │ └──────────────────────────────────────────────────┘  │ │ │
│  │  │ ┌──────────────────────────────────────────────────┐  │ │ │
│  │  │ │ API Routes (/api/*)                             │  │ │ │
│  │  │ │ • /api/fabric/invoke                            │  │ │ │
│  │  │ │ • /api/fabric/query                             │  │ │ │
│  │  │ │ • /api/health                                   │  │ │ │
│  │  │ │ • /api/blockchain/*                             │  │ │ │
│  │  │ └──────────────────────────────────────────────────┘  │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  │                           │                                  │ │
│  │                           │ Internal Network                 │ │
│  │                           ▼                                  │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │ Docker Network: traceroot-network                     │ │ │
│  │  │                                                        │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ PORT 7054: Certificate Authority (CA)           │ │ │ │
│  │  │  │ • Generates digital certificates                │ │ │ │
│  │  │  │ • Enrolls users and admins                       │ │ │ │
│  │  │  │ Image: hyperledger/fabric-ca:1.5                │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  │                           │                            │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ PORT 7050: Orderer (Consensus)                  │ │ │ │
│  │  │  │ • Collects transactions                          │ │ │ │
│  │  │  │ • Creates blocks                                 │ │ │ │
│  │  │  │ • Maintains order                                │ │ │ │
│  │  │  │ Image: hyperledger/fabric-orderer:2.5           │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  │                           │                            │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ PORT 7051: Peer (Ledger)                        │ │ │ │
│  │  │  │ • Stores blockchain ledger                       │ │ │ │
│  │  │  │ • Executes chaincode (smart contracts)           │ │ │ │
│  │  │  │ • Validates transactions                         │ │ │ │
│  │  │  │ Image: hyperledger/fabric-peer:2.5              │ │ │ │
│  │  │  │                                                  │ │ │ │
│  │  │  │  ┌────────────────────────────────────────────┐ │ │ │ │
│  │  │  │  │ Ledger Database (LevelDB/CouchDB)          │ │ │ │ │
│  │  │  │  │ • World State (current data)               │ │ │ │ │
│  │  │  │  │ • Transaction History                      │ │ │ │ │
│  │  │  │  └────────────────────────────────────────────┘ │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  │                           │                            │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ Chaincode (Smart Contracts)                     │ │ │ │
│  │  │  │ • Product tracking logic                         │ │ │ │
│  │  │  │ • Supply chain operations                        │ │ │ │
│  │  │  │ • Business rules                                 │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  │                                                        │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  │                                                              │ │
│  └──────────────────────────────────────────────────────────────┘ │
│                                                                     │
└────────────────────────────────────────────────────────────────────┘
```

---

## 📋 File Structure After Replit Setup

```
TraceRoot/
│
├── 📄 .replit                          ← Replit environment config
│
├── 📚 Setup Guides (NEW)
│   ├── QUICK_START_REPLIT.md          ← START HERE! (5 mins)
│   ├── DEPLOYMENT_COMPLETE.md         ← Summary (you are here)
│   ├── REPLIT_DEPLOYMENT_GUIDE.md     ← Detailed guide
│   ├── REPLIT_SETUP_README.md         ← Full overview
│   └── HEALTH_CHECK_EXAMPLE.js        ← API example
│
├── 🚀 Setup Scripts (NEW)
│   ├── AUTO_SETUP.sh                  ← One-click setup (best!)
│   ├── REPLIT_SETUP.sh                ← Alternative setup
│   └── docker-compose.*.yml           ← Fabric configs
│
├── 📦 Your Original Project
│   ├── package.json
│   ├── next.config.mjs
│   ├── tsconfig.json
│   │
│   ├── 🌐 app/
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── api/                       ← Your API endpoints
│   │   │   ├── fabric/
│   │   │   │   ├── enroll/
│   │   │   │   └── register/
│   │   │   ├── auth/
│   │   │   ├── blockchain/
│   │   │   └── [other endpoints]
│   │   └── [routes]
│   │
│   ├── 🧩 components/
│   │   ├── layout/
│   │   ├── forms/
│   │   └── [UI components]
│   │
│   ├── 🔗 lib/
│   │   └── [utilities]
│   │
│   ├── 🖼️ public/
│   │   └── [images, assets]
│   │
│   ├── 🎨 styles/
│   │   └── globals.css
│   │
│   ├── 🧪 test/
│   │   └── [test files]
│   │
│   ├── 🔐 fabric-network/
│   │   ├── docker-compose.yml         ← Fabric configuration
│   │   ├── network.sh                 ← Start/stop script
│   │   ├── connection-profile.json    ← Network config
│   │   ├── crypto-config/             ← Certificates & keys
│   │   └── system-genesis-block/      ← Blockchain genesis
│   │
│   ├── ⛓️  chaincode/
│   │   └── traceroot/                 ← Smart contracts
│   │
│   ├── 📜 scripts/
│   │   ├── enroll.js                  ← Register users
│   │   ├── deploy-chaincode.js        ← Deploy contracts
│   │   ├── test-fabric.js             ← Test network
│   │   └── [other scripts]
│   │
│   └── 📋 [other files]
│
└── 📖 Documentation
    ├── README.md                      ← Original project info
    ├── LICENSE
    └── [config files]
```

---

## 🔄 Setup Flow Diagram

```
START
  │
  ▼
┌─────────────────────────────────────┐
│ 1. Create Replit Account            │
│    (replit.com, NO credit card)     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 2. Import TraceRoot Project         │
│    (from GitHub or upload)          │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 3. Open Replit Terminal             │
│    (in browser)                     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 4. Run Auto Setup Script            │
│    bash AUTO_SETUP.sh               │
│    (fully automated)                │
└──────────────┬──────────────────────┘
               │
      ┌────────┴────────┐
      │                 │
      ▼                 ▼
   ✅ SUCCESS       ❌ FAILS
      │                 │
      │                 ▼
      │        ┌───────────────────┐
      │        │ Use Minimal Config │
      │        │ docker-compose.   │
      │        │ minimal.yml       │
      │        └─────────┬─────────┘
      │                  │
      │                  ▼
      │        ┌───────────────────┐
      │        │ Still Not Working?│
      │        │ Use Ultra-Minimal │
      │        │ docker-compose.   │
      │        │ ultra-minimal.yml │
      │        └─────────┬─────────┘
      │                  │
      └────────┬─────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 5. Start Next.js App                │
│    npm run dev                      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 6. Get Your Replit URL              │
│    https://project.replit.dev       │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│ 7. Share with Reviewers             │
│    Send them the URL!               │
└──────────────┬──────────────────────┘
               │
               ▼
              ✅
         PROJECT ACCEPTED!
```

---

## ⏱️ Timeline Breakdown

```
Action                          Time      Running?
─────────────────────────────────────────────────────
Create Replit Account           2 min     You
Import Project                  1 min     You
Run AUTO_SETUP.sh:
  ├─ npm install                1 min     Auto
  ├─ Docker setup               ~10s      Auto
  ├─ Start CA                   ~30s      Auto (Docker)
  ├─ Start Orderer              ~30s      Auto (Docker)
  ├─ Start Peer                 ~60s      Auto (Docker)
  ├─ Create Channel             ~30s      Auto (Docker)
  └─ Enroll Users               ~30s      Auto (Fabric)
                         ───────────────
Total Setup Time:               ~3-4 min
                         ───────────────

npm run dev                     ~30s      Auto

Total Until Live:              ~4-5 min   ✅ LIVE!
```

---

## 🔌 Port Mapping

When running on Replit:

```
Inside Container          External (Replit URL)
─────────────────────────────────────────────────
3000 (Next.js)      →    replit.dev (port 80)
7054 (CA)           →    replit.dev:7054
7050 (Orderer)      →    replit.dev:7050
7051 (Peer)         →    replit.dev:7051
7052 (Peer CC)      →    replit.dev:7052
```

Configured in `.replit` file automatically.

---

## 🎯 What Each Component Does

### 🌐 Next.js (Frontend + API)
- **Port**: 3000 → 80 (via Replit)
- **Purpose**: Web interface + REST API
- **Your Code**: In `/app` directory
- **Tech**: React, TypeScript, Tailwind CSS

### 🔐 Hyperledger Fabric Network
- **Components**:
  - **CA** (7054): Issues digital certs
  - **Orderer** (7050): Consensus, creates blocks
  - **Peer** (7051): Stores data, runs contracts
  - **Ledger**: Immutable blockchain database

### ⛓️ Chaincode (Smart Contracts)
- **Purpose**: Business logic on blockchain
- **Location**: `/chaincode/traceroot`
- **Language**: Typically Go or Node.js
- **Function**: Execute transactions, manage state

---

## 🛡️ Security Notes

⚠️ **This is for DEMO purposes**

For production, you'd need:
- ✅ HTTPS certificates
- ✅ User authentication
- ✅ Backup & disaster recovery
- ✅ Monitoring & alerts
- ✅ Multiple orderers
- ✅ Multiple peers (HA)

But for showing reviewers? Perfect as-is! 🎉

---

## 💾 Data Persistence

```
ON YOUR MACHINE          →    ON REPLIT
─────────────────────────────────────────
Local database                Replit filesystem
Chaincode files               Docker volumes
Certificates                  Docker volumes
```

**Important**: Back up your wallet files!

```bash
# Before deploying, backup:
fabric-network/crypto-config/
fabric-network/wallet/
```

---

## 📊 Resource Usage

```
Component       Memory    CPU    Disk
──────────────────────────────────────
Next.js         ~100MB    Low    ~50MB
CA              ~200MB    Low    ~100MB
Orderer         ~300MB    Med    ~200MB
Peer            ~400MB    Med    ~500MB
──────────────────────────────────────
TOTAL           ~1GB      Med    ~900MB

Replit Limits:
  • Memory: 512MB - 2GB (free tier)
  • Disk: 5GB
  • CPU: Shared

✅ You should be fine!
```

---

## 🎓 Next Steps After Deployment

1. **Test API endpoints** via browser/Postman
2. **Verify blockchain** is accepting transactions
3. **Check logs** for any errors: `docker logs <container>`
4. **Share URL** with reviewers
5. **Get feedback** and iterate
6. **Celebrate!** 🎉

---

## 📞 Quick Help Links

| Issue | Solution |
|-------|----------|
| Can't start setup | Check: QUICK_START_REPLIT.md |
| Docker fails | Try: docker-compose.minimal.yml |
| API is slow | Normal on free tier (1-2 sec) |
| Need details | Read: REPLIT_DEPLOYMENT_GUIDE.md |
| Everything works! | Share URL with reviewers! 🚀 |

---

**You're all set! Follow QUICK_START_REPLIT.md and you'll be live in 5 minutes! 🚀**
